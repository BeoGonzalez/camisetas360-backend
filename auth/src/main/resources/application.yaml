server:
  port: 83 # El microservicio operará en este puerto de forma local

spring:
  application:
    name: auth-service

  # Configuración de seguridad para validación de tokens de Azure Entra ID
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://login.microsoftonline.com/
          tenant-id: e5372bf0-c5e3-4286-887c-79069f209c1f